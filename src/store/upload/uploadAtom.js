import { atom } from "recoil";

export const uploadFileUrl = atom({
    key : "uploadFileUrlAtom",
    default : null,
})

export const tagAtom = atom({
    key:"tagAtom",
    default:"Design",   
})

